# This is a Databricks asset bundle definition for my_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: my_project

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2185313560766934.14.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-2185313560766934.14.azuredatabricks.net
      # We explicitly specify /Users/duraishanmugaraj.r@gmail.com to make sure we only have a single copy.
      root_path: /Users/duraishanmugaraj.r@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: duraishanmugaraj.r@gmail.com
        level: CAN_MANAGE
    run_as:
      user_name: duraishanmugaraj.r@gmail.com
